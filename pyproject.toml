[build-system]
requires = ["hatchling>=1.26", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "af-credentials"
authors = [
  { name = "Giordon Stark", email = "kratsg@gmail.com" },
]
description = "Backend-side client for the AF MCP credential broker: identity-token verification and x509 proxy redemption"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.10"
classifiers = [
  "Development Status :: 1 - Planning",
  "Intended Audience :: Science/Research",
  "Intended Audience :: Developers",
  "Operating System :: OS Independent",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Scientific/Engineering",
  "Typing :: Typed",
]
dynamic = ["version"]
dependencies = [
  "httpx2>=2.5.0,<3",
  "pyjwt[crypto]>=2.8",
]

[project.optional-dependencies]
mcp = ["mcp>=2.0.0,<3"]

[project.urls]
Documentation = "https://af-credentials.readthedocs.io/"
Homepage = "https://github.com/maniaclab/af-credentials"
"Bug Tracker" = "https://github.com/maniaclab/af-credentials/issues"
Discussions = "https://github.com/maniaclab/af-credentials/discussions"
Changelog = "https://github.com/maniaclab/af-credentials/releases"


[dependency-groups]
test = [
  "pytest >=8",
  "pytest-cov >=5",
  "pytest-asyncio >=0.24",
  "mcp >=2.0.0,<3",  # so the `[mcp]` adapter's tests run for real, not via pytest.importorskip's skip path
]
dev = [
  { include-group = "test" },
]
docs = [
    "zensical>=0.0.20",
]


[tool.hatch]
version.source = "vcs"
build.hooks.vcs.version-file = "src/af_credentials/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/af_credentials"]

[tool.pytest]
minversion = "8.0"
addopts = ["-ra", "--showlocals", "--strict-markers", "--strict-config"]
strict = true
filterwarnings = [
  "error",
]
log_level = "INFO"
testpaths = [
  "tests",
]
asyncio_mode = "auto"


[tool.coverage]
run.source = ["af_credentials"]
report.exclude_also = [
  '\.\.\.',
  'if typing.TYPE_CHECKING:',
]

[tool.mypy]
files = ["src", "tests"]
python_version = "3.10"
warn_unused_configs = true
strict = true
enable_error_code = ["ignore-without-code", "redundant-expr", "truthy-bool"]
warn_unreachable = true
disallow_untyped_defs = false
disallow_incomplete_defs = false

[[tool.mypy.overrides]]
module = "af_credentials.*"
disallow_untyped_defs = true
disallow_incomplete_defs = true


[tool.ruff]
show-fixes = true

[tool.ruff.lint]
extend-select = [
  "ARG",    # flake8-unused-arguments
  "B",      # flake8-bugbear
  "BLE",    # flake8-blind-except
  "C4",     # flake8-comprehensions
  "DTZ",    # flake8-datetimez
  "EM",     # flake8-errmsg
  "EXE",    # flake8-executable
  "FA",     # flake8-future-annotations
  "FLY",    # flynt
  "FURB",   # refurb
  "G",      # flake8-logging-format
  "I",      # isort
  "ICN",    # flake8-import-conventions
  "ISC",    # flake8-implicit-str-concat
  "LOG",    # flake8-logging
  "NPY",    # NumPy specific rules
  "PD",     # pandas-vet
  "PERF",   # perflint
  "PGH",    # pygrep-hooks
  "PIE",    # flake8-pie
  "PL",     # pylint
  "PT",     # flake8-pytest-style
  "PTH",    # flake8-use-pathlib
  "PYI",    # flake8-pyi
  "Q",      # flake8-quotes
  "RET",    # flake8-return
  "RSE",    # flake8-raise
  "RUF",    # Ruff-specific
  "SIM",    # flake8-simplify
  "SLOT",   # flake8-slots
  "T10",    # flake8-debugger
  "T20",    # flake8-print
  "TC",     # flake8-type-checking
  "TRY",    # tryceratops
  "UP",     # pyupgrade
  "YTT",    # flake8-2020
]
ignore = [
  "PLR09",    # Too many <...>
  "PLR2004",  # Magic value used in comparison
  "PLC0415",  # Lazy imports are deliberate (the `mcp` extra's optional import)
  "TRY003",   # Long exception messages inline — custom exception classes would be over-engineering
  "EM101",    # String literal in exception — assign-to-variable pattern adds noise, not clarity
  "EM102",    # f-string in exception — same rationale as EM101
  "ISC001",   # Conflicts with the ruff formatter (implicit string concatenation)
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["ARG"]


[tool.pylint]
py-version = "3.10"
ignore-paths = [".*/_version.py"]
reports.output-format = "colorized"
similarities.ignore-imports = "yes"
messages_control.disable = [
  "broad-exception-caught",
  "design",
  "duplicate-code",
  "fixme",
  "line-too-long",
  "missing-module-docstring",
  "missing-function-docstring",
  "wrong-import-order",
  "ungrouped-imports",
  "wrong-import-position",
]
